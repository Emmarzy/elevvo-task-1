# Elevvo Internship - Task 1

## Data Requirements
- **Dataset:** `StudentPerformanceFactors.csv`
- **Expected local path:** place the file at workspace root so notebook paths resolve (for example `../../StudentPerformanceFactors.csv` from this task folder).

## Repository Data Policy
- Do **not** commit large/raw dataset files unless specifically required.
- Keep notebooks/code in the repo and document dataset source + placement.
- If sharing publicly, include only small sample data (optional) and keep full data local.

## Quick Setup
1. Download/obtain `StudentPerformanceFactors.csv`.
2. Place it in the project root (`Machine Learning Track/`).
3. Run `Task_1_Solution.ipynb`.
